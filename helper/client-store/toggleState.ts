import create from "zustand";
import { IStopCounter } from "../Typeface";

// export const useStopCounter = create<IStopCounter>((set) => ({
//   stopCounter: false,
//   setStopCounter: () =>
//     set(({ stopCounter }) => ({ stopCounter: !stopCounter })),
// }));
